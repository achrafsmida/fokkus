{% extends 'base.html.twig' %}
{% block body %}
    {#<div class="row">#}
        {#<div class="col-lg-12">#}
            {#<div class="panel panel-default">#}
                {#<div class="panel-heading">#}
                    {#Dashboard |#}
                    {#<small>Messages lus</small>#}
                    {#<a class="btn btn-xs btn-success pull-right" href="{{ path('message_new') }}">Create a new#}
                        {#message</a>#}

                {#</div>#}
                {#<div class="panel-body">#}
                    {#<table id="datatable2" class="table table-striped table-hover">#}
                        {#<thead>#}
                        {#<tr>#}
                            {#<th>Subject</th>#}
                            {#<th>Message</th>#}
                            {#<th>Sender</th>#}
                            {#<th>Created date</th>#}
                            {#<th>Actions</th>#}
                        {#</tr>#}
                        {#</thead>#}
                        {#<tbody>#}
                        {#{% for message in messages %}#}
                            {#{% if message.readed %}#}
                                {#{% for user in  message.users %}#}
                                    {#{% if user.id  == app.user.getId() %}#}
                                        {#<tr>#}
                                        {#<td>#}
                                            {#{{ message.message }} </td>#}
                                        {#<td>{{ message.subject }}</td>#}
                                        {#<td>{{ message.sender.username }}</td>#}
                                        {#<td>{% if message.createdDate %}{{ message.createdDate|date('Y-m-d H:i:s') }}{% endif %}</td>#}
                                        {#<td>#}
                                            {#<a class="btn btn-info"#}
                                               {#href="{{ path('message_edit', { 'id': message.id }) }}">Edit</a>#}
                                            {#<a class="btn btn-success"#}
                                               {#href="{{ path('message_show', { 'id': message.id }) }}">Show</a>#}
                                            {#<a class="btn btn-danger"#}
                                               {#href="{{ path('message_delete', { 'id': message.id }) }}">Delete</a>#}
                                        {#</td>#}
                                        {#</tr>{% endif %}#}
                                {#{% endfor %} {% endif %} {% endfor %}#}
                        {#</tbody>#}
                    {#</table>#}

                {#</div>#}
            {#</div>#}
        {#</div>#}
    {#</div>#}

    <section>
        <!-- Page content-->
        <div class="content-wrapper">
            <div class="content-heading">
                <div class="pull-right"><a href="" class="btn btn-sm btn-default text-sm">&lt; back</a>
                </div>Forum - Discussion
                <small>Topic ID: 999</small>
            </div>
            <div class="panel panel-default">
                <table class="table table-striped">
                    <tbody>
                    {% for message in messages %}
                        {% if message.readed %}
                            {% for user in  message.users %}
                                {% if user.id  == app.user.getId() %}
                    <tr>
                        <td class="text-center">
                            <a href="">
                                <strong>{{ message.sender.username }}</strong>
                            </a>
                        </td>
                        <td class="text-right">on
                            <em>{{ message.createdDate|date('Y-m-d H:i:s') }}</em>
                        </td>
                    </tr>
                    <tr>
                        <td class="text-center">
                            <div class="mt">
                                <a href="">
                                    <img src="{{ asset('bundles/img/04.jpg') }}" alt="avatar" class="img-circle thumb64">
                                </a>
                            </div>
                            <small>{{ message.sender.username }}</small>
                            <br>
                            <small>Since {{ message.createdDate|date('Y-m-d H:i:s') }}</small>
                        </td>
                        <td>
                           <h3>{{ message.subject }}</h3>
                            <p>{{ message.message }}</p>
                            <div class="text-right">
                                <a class="btn btn-danger" href="{{ path('deleted_message', { 'id': message.id }) }}">Delete</a><em style="margin: 15px">Signed by {{ message.sender.username }}</em>
                            </div>
                        </td>
                    </tr>{% endif %}
                    {% endfor %} {% endif %} {% endfor %}
                    </tbody>
                </table>
            </div>

            <div id="topic-reply" class="collapse">
                <div class="text-center">
                    <small>Make sure you have read the <a href="">forum rules </a>before posting</small>
                </div>
                <div class="row">
                    <div class="col-md-8 col-md-offset-2">
                        <form action="/" method="post" class="form-horizontal">
                            <div class="form-group">
                                <div class="col-md-12">
                                    <textarea id="reply-message" name="reply-message" rows="5" placeholder="Type your answer..." class="form-control"></textarea>
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-md-12 text-center">
                                    <button type="button" class="btn btn-sm btn-primary">Post Answer</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
