{% extends 'base.html.twig' %}

{% block body %}
    <h1>Networks list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Haveuser</th>
                <th>Createddate</th>
                <th>Updateddate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for network in networks %}
            <tr>

                <td>{{ network.sub.lastName }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('network_show', { 'id': network.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('network_edit', { 'id': network.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('network_new') }}">Create a new network</a>
        </li>
    </ul>
{% endblock %}
