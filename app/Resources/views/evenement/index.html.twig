{% extends 'base.html.twig' %}

{% block body %}
     <div class="row">
                  <div class="col-lg-12">
                     <div class="panel panel-default">
                        <div class="panel-heading">
                           Events |
                           <small>List</small>
                                       <a class="btn btn-success btn-xs pull-right" href="{{ path('evenement_new') }}"><span class="icon-plus"></span> Create a new formation</a>
                                       
                        </div>
                        <div class="panel-body">

      <table id="datatable2" class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for evenement in evenements %}
            <tr>
                <td>{{ evenement.nom }}</td>
                <td>
                   
                            <a class="btn btn-info"  href="{{ path('evenement_show', { 'id': evenement.id }) }}"><span class="icon-eye"></span> show</a>
                      
                            <a class="btn btn-warning"  href="{{ path('evenement_edit', { 'id': evenement.id }) }}">  <span class="icon-wrench">  edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

  
{% endblock %}
