{% extends 'base.html.twig' %}

{% block body %}
          <div class="row">
                  <div class="col-lg-12">
                     <div class="panel panel-default">
                        <div class="panel-heading">
                          Evenement | add  |
                           <small></small>
                                       <a class="btn btn-success btn-xs pull-right" href="{{ path('evenement_index') }}">Evenement list</a>
 
                        </div>
                        <div class="panel-body">

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

  
{% endblock %}
