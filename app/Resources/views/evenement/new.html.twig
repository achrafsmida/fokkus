{% extends 'base.html.twig' %}

{% block body %}
          <div class="row">
                  <div class="col-lg-12">
                     <div class="panel panel-default">
                        <div class="panel-heading">
                          Events | add  |
                           <small></small>
                                       <a class="btn btn-info btn-xs pull-right" href="{{ path('evenement_index') }}">                            <span class="icon-list"></span>
 Evenement list</a>
 
                        </div>
                        <div class="panel-body">

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-success btn-xs"  style="margin-top:20px" value="Create" />
    {{ form_end(form) }}

  
{% endblock %}
