{% extends 'base.html.twig' %}

{% block body %}
       <div class="row">
                  <div class="col-lg-12">
                     <div class="panel panel-default">
                        <div class="panel-heading">
                           Evenement category |
                           <small>List</small>
                                       <a class="btn btn-success btn-xs pull-right" href="{{ path('evenementsubcateg_new') }}">Create a new sub catégory</a>

                        </div>
                        <div class="panel-body">

      <table id="datatable2" class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for evenementsubcateg in evenementsubcategs %}
            <tr>
                <td><a href="{{ path('evenementsubcateg_show', { 'id': evenementsubcateg.id }) }}">{{ evenementsubcateg.id }}</a></td>
                <td>{{ evenementsubcateg.nom }}</td>
                <td>
                   
                            <a class="btn btn-info" href="{{ path('evenementsubcateg_show', { 'id': evenementsubcateg.id }) }}">show</a>
                     
                            <a class="btn btn-warning" href="{{ path('evenementsubcateg_edit', { 'id': evenementsubcateg.id }) }}">edit</a>
                        
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('evenementsubcateg_new') }}">Create a new evenementsubcateg</a>
        </li>
    </ul>
{% endblock %}
