{% extends 'base.html.twig' %}

{% block body %}
          <div class="row">
                  <div class="col-lg-12">
                     <div class="panel panel-default">
                        <div class="panel-heading">
                          Evenement  sub category  | ADD  |
                           <small></small>
                                       <a class="btn btn-success btn-xs pull-right" href="{{ path('evenementsubcateg_index') }}">Evenement sub category list</a>
 
                        </div>
                        <div class="panel-body">

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn-success btn-xs" type="submit" value="Create" />
    {{ form_end(form) }}

  
{% endblock %}
