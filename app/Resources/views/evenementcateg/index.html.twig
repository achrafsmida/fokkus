{% extends 'base.html.twig' %}

{% block body %}
       <div class="row">
                  <div class="col-lg-12">
                     <div class="panel panel-default">
                        <div class="panel-heading">
                           Evenement category |
                           <small>List</small>
                                       <a class="btn btn-success btn-xs pull-right" href="{{ path('evenementcateg_new') }}">Create a new category</a>

                        </div>
                        <div class="panel-body">

      <table id="datatable2" class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for evenementcateg in evenementcategs %}
            <tr>
                <td><a href="{{ path('evenementcateg_show', { 'id': evenementcateg.id }) }}">{{ evenementcateg.id }}</a></td>
                <td>{{ evenementcateg.nom }}</td>
                <td>
                  
                            <a class="btn btn-info" href="{{ path('evenementcateg_show', { 'id': evenementcateg.id }) }}">show</a>
                        
                            <a class="btn btn-warning"   href="{{ path('evenementcateg_edit', { 'id': evenementcateg.id }) }}">edit</a>
                        
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

  
{% endblock %}
