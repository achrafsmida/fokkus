{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
                  <div class="col-lg-12">
                     <div class="panel panel-default">
                        <div class="panel-heading">
                           TIME LINE |
                           <small>{{ app.session.get('typestring')}}| GROUPS</small>
                                       <a class="btn btn-success btn-xs pull-right" href="{{ path('groups_new') }}">Create a new groups</a>

                        </div> 
                        <div class="panel-body">

      <table id="datatable2" class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Name</th>
      
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for group in groups %}
            <tr>
                <td>{{ group.name }}</td>
               
                <td>
                    <a class="btn btn-info bnt-xs" href="{{ path('step_index', { 'type':app.session.get('type')   , 'group' :group.id }) }}">Step List</a>
                     
                      
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

  
{% endblock %}
