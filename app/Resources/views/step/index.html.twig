{% extends 'base.html.twig' %}

{% block body %}

    
          <div class="row">
                  <div class="col-lg-12">
                     <div class="panel panel-default">
                        <div class="panel-heading">
                           Timeline |
                           <small>{{ app.session.get('typestring')}}</small>
                                       <a class="btn btn-success btn-xs pull-right" href="{{ path('step_new') }}"><span class="icon-plus"></span> Create a new step</a>
                                       <a style ="margin-left:10px;margin-right:10px" class="btn btn-info btn-xs pull-right" href="{{ path('step_visualtion') }}"><span class="icon-eyeglass"> Visulation</a>

                        </div>
                        <div class="panel-body">
                           <table id="datatable2" class="table table-striped table-hover">
                              <thead>
                                    <th>Id</th>
                                    <th>Name</th>
                                    <th>Date created</th>
                                    <th>Date updated</th>
                                    <th>Actions</th>
                              </thead>
                              <tbody>
                                 {% for step in steps %}
            <tr>
                <td><a href="{{ path('step_show', { 'id': step.id }) }}">{{ step.id }}</a></td>
                <td>{{ step.name }}</td>
                <td>{% if step.datecreated %}{{ step.datecreated|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if step.dateupdated %}{{ step.dateupdated|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    
                        
                            <a class="btn btn-warning" href="{{ path('step_edit', { 'id': step.id }) }}"> <span class="icon-wrench"> edit</a>
                            <a class="btn btn-info" href="{{ path('sousstep_index', { 'step': step.id }) }}"><span class="icon-list"></span> sub step list</a>

                </td>
            </tr>
        {% endfor %}
                              </tbody>
                             
                           </table>
                        </div>
                     </div>
                  </div>
               </div>
           
    
    
   
{% endblock %}
