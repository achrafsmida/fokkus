{% extends 'base.html.twig' %}

{% block body %}
          <div class="row">
                  <div class="col-lg-12">
                     <div class="panel panel-default">
                        <div class="panel-heading">
                          Consulting  | EDIT  |
                           <small></small>
                                       <a class="btn btn-info btn-xs pull-right" href="{{ path('formation_index') }}">                            <span class="icon-list"></span>
 Formation list</a>
 
                        </div>
                        <div class="panel-body">

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-warning btn-xs"  style="margin-top:20px" type="submit" value="Edit" />
    {{ form_end(edit_form) }}

   
{% endblock %}
