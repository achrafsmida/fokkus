{% extends 'base.html.twig' %}

{% block body %}
      <div class="row">
                  <div class="col-lg-12">
                     <div class="panel panel-default">
                        <div class="panel-heading">
                           Formation |
                           <small>List</small>
                                       <a class="btn btn-success btn-xs pull-right" href="{{ path('formation_new') }}"><span class="icon-plus"></span> Create a new formation</a>

                        </div>
                        <div class="panel-body">

      <table id="datatable2" class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Catégorie</th>
                <th>Prix</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for formation in formations %}
            <tr>
                <td>{{ formation.nom }}</td>
                <td>{{ formation.formationcateg }}</td>
                <td>{{ formation.prix }}</td>
                <td>
                   
                            <a class="btn btn-info" href="{{ path('formation_show', { 'id': formation.id }) }}"><span class="icon-eye"></span> show</a>
                       
                            <a class="btn btn-warning" href="{{ path('formation_edit', { 'id': formation.id }) }}"><span class="icon-wrench"> edit</a>
                        
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

  
{% endblock %}
